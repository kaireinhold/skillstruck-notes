// How to declare a variable:
var myname = "Kai";
// How to output to console:
console.log(myname);
// Data types:
var is_string = "string";
var is_integer = 10;
var is_boolean = true; //or false
var is_null = null; //null essentially means that the variable is empty.
console.log(is_string, is_integer, is_boolean, is_null) //if you run this, the console will output 'string 10 true'. See how the null variable isn't technically shown? That's because it's empty.